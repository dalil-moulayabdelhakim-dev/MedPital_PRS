// Set the target date and time for the countdown
// const parag = document.getElementById('countdown');
// var deleteTimestamp = parag.getAttribute('data-time'); // Convert seconds to milliseconds

// // Update the countdown every second
// var countdownInterval = setInterval(function () {
//     //Decrease the duration by 1 second
//     deleteTimestamp--;

//     // Display the remaining time
//     parag.innerHTML = 'Account will be deleted in ' + deleteTimestamp + ' seconds';

//     // If the countdown is over, clear the interval
//     if (deleteTimestamp < 0) {
//         clearInterval(countdownInterval);
//         parag.innerHTML = 'Account deleted';
//     }
// }, 1000);
